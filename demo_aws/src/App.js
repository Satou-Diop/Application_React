import { withAuthenticator } from '@aws-amplify/ui-react';
import './App.css';

function App() {
  return (
    <div >
      <header >
        
      </header>
    </div>
  );
}

export default withAuthenticator(App);
